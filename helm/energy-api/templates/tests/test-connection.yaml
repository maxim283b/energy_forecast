apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "energy-api.fullname" . }}-test-connection"
  labels:
    {{- include "energy-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "energy-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
