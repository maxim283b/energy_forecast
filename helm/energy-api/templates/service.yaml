apiVersion: v1
kind: Service
metadata:
  name: {{ include "energy-api.fullname" . }}
  labels:
    {{- include "energy-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "energy-api.selectorLabels" . | nindent 4 }}
